{
  "name": "linkedin-post-generator",
  "version": "1.0.0",
  "private": true,
  "description": "Generates deterministic LinkedIn PDF carousels, infographics and code-snippet images from a JSON slide manifest. Sibling pipeline to video-generator, swapping the window.__seek(t) time contract for CSS @page pagination and headless-Chromium page.pdf()/screenshot() export.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "render": "node scripts/render.mjs",
    "quality-gate": "node scripts/quality-gate.mjs"
  },
  "dependencies": {
    "@terrastruct/d2": "^0.1.33",
    "@typesafe-ai/sdk": "^0.6.0",
    "mermaid": "^11.4.1",
    "playwright": "^1.56.0",
    "shiki": "^1.24.0",
    "zod": "^4.6.5"
  }
}
